# 通用项目流程 Spec

> **版本**：v0.1（草案）
> **创建时间**：2026-09-14
> **状态**：**待批准**
> **定位**：通用项目流程设计——适用于一切"外部自动化 / patch / crack / 逆向复刻 / 集成探测"类项目
> **与 Teamwork-Guideline 的关系**：**独立文件，暂不修改 Teamwork-Guideline**（老公指令）；验证有效后作为候选流程并入或替换其流程章节
> **实证来源**：Trae daily-checkin 项目 2026-09-12~14 的弯路与纠正（四版 spec 被驳回的完整过程）+ WorkBuddy 先例项目的方法论还原

---

## 1. 目的

解决一个已实证的核心问题：**spec 建立在残缺探测之上，是项目失败与反复返工的第一根因**。

本 spec 在"讨论 → 对齐 → spec → 执行计划"的既有流程中，**前置并制度化 Probe（探测）阶段**，使 spec 的机制层结论只来自分级证据，而非静态推断或想象。

## 2. 流程总览

```
S0 讨论（目标与意图）
  → S1 P1 摸底 probe（先例盘点 + 无预设基线）
  → S2 对齐（基于 P1 素材，确认方法论与边界）
  → S3 P2 全面 probe（范围内穷尽 + 对抗复核）
  → S4 spec（机制层全部来自 L1/L2 证据）
  → S5 执行计划
  → S6 执行 ⇄ P3 定向 probe 穿插（授权门 + 回环规则）
  → S7 交付闭环
```

**两条铁律**：
1. **机制不明不动手**——S4 spec 的每个机制结论必须标注 L1/L2 级证据（§5），否则退回 S3；
2. **不预设方向**——禁止机制结论预设；范围界定（目标边界）必须，方向预设（机制猜想当前提）禁止。

---

## 3. 阶段定义

### S0 讨论
决策者陈述目标与意图；agent 复述确认（不假设同义，语义以决策者引用的先例为准）。产出：目标一句话 + 约束初稿。

### S1 — P1 摸底 probe
**定位**：无预设方向的基线建立，为对齐提供素材。

| 项 | 规范 |
|---|---|
| **第 0 步（强制）** | **先例盘点**：查找并精读已验证先例——交付物真身（代码逐行）、计划文档、HANDOFF、关键决策原文。有先例则其方法论为默认模板 |
| 允许 | 本地文件/存储只读扫描；源码静态定位（入口/凭证/关键词）；环境基线（版本/路径/哈希）；目标软件自然行为观测 |
| 禁止 | 机制结论预设；深层手段开局（抓包/逆向加密/网络栈指纹）；任何写操作与真实动作触发 |
| 产出 | 先例清单、入口全枚举、环境基线、假设清单（全部标注 L3/L4 级） |
| 退出准则 | 先例盘点完成 + 入口枚举覆盖目标范围 + 基线落盘 |
| 时间盒 | 必须设定（防止无限摸底） |

### S2 — 对齐
基于 P1 素材，决策者与 agent 确认：目标语义、方法论模板（若有先例）、约束清单、探测边界。**决策者的纠正 = 语义对齐信号**，逐条复述确认。

### S3 — P2 全面 probe
**定位**：范围声明内穷尽机制事实，排除 P1 误判。

| 项 | 规范 |
|---|---|
| 手段梯度 | 由浅入深：运行时拦截（hook fetch/XHR/关键函数）→ 调试器真值 → （仅当前者穷尽且需鉴权/实现层分析时）抓包等深层手段。**静态分析降为辅助**（定位与假设，不作机制结论） |
| "全面"的定义 | **范围声明内穷尽**：全部候选入口 × 全部候选机制假设，均获 L1/L2 级证实或证伪记录；不做无限探索 |
| 误判排除 | **对抗性复核**：P2 产物交独立 agent/审计者复核（相当于 red-team），不自我复查 |
| 产出 | 机制规格（完整请求/调用链/凭证流转）+ 假设裁决表（每项带证据级别）+ 退出申请 |
| 退出准则 | 机制规格完整 + 假设全部裁决 + 对抗复核通过 |
| 时间盒 | 必须设定 |

### S4 — spec
机制层结论**只采信 L1/L2**；L3/L4 仅可作交叉参考与背景。接口契约（CLI/退出码/输出格式）等设计决策可继承既有已批准版本，但须显式区分"设计决策"与"机制推断"。产出后交独立审计。

### S5 — 执行计划
按 spec 分解，含授权门位置、测试矩阵（含合规自检项）、备份与回滚方案。

### S6 — 执行 ⇄ P3 定向 probe 穿插
**定位**：执行期按需的定向探测，无固定顺序，随项目进度穿插。

| 项 | 规范 |
|---|---|
| 触发条件 | 执行中遇到机制盲区 / 假设被现实证伪 / 需定位新入口 |
| 授权门 | 定向 probe 若涉及真实动作（写操作/消耗性动作），必须先过决策者授权 |
| 回环规则 | P3 发现若推翻 P2/spec 结论 → **回环 S3 复核 → 更新 spec**，禁止"执行中静默改机制认知" |
| 产出 | 定向探测报告（带证据级别）+ 对 spec 的修订建议 |

### S7 — 交付闭环
独立交付物 + 只读验证 + 授权后单次真实动作验证 + 幂等验证 + 存储无写入复核（哈希对比）+ 自审 + 本地 commit（等指令）+ 记忆收尾（Engram / session summary）。衍生目标（如鉴权续期）**独立立项**，不静默扩 scope。

---

## 4. 失败处理原则

1. **失败 = 探测不完整的信号，不是路线死亡的信号**——正确动作是回 S3 补探测，而非在 S6 实现层反复换参数；
2. **单一归因禁止**——失败根因必须多假设并行，设计区分性实验裁决；
3. **穷尽手段仍不可行**——带完整证据链呈报决策者重新决策，**agent 不自行收敛路线**；
4. **回溯方法论而非修补实现**——失败后第一问："先例是怎么发现机制的？我跳过了哪一步？"

---

## 5. 证据分级与采信规则

| 级别 | 类型 | 采信度 |
|---|---|---|
| **L1** | 物理观测：运行时拦截捕获、调试器真值、文件哈希、DNS/连接观测 | **唯一推论基准** |
| **L2** | 可复现实验：受控重放、对照变体、只读验证闭环 | 高，机制判定依据 |
| L3 | 静态分析：源码阅读、本地文件内容 | 中——定位与假设用，**不单独定论** |
| L4 | 历史产物：既有 spec 结论、既往探测报告 | 仅交叉验证，**禁止作为推论前提** |
| U | 用户实证：亲测行为、先例陈述、截图 | 用户权威事实，直接采信 |

规则：L3/L4 与 L1/L2 冲突时以 L1/L2 为准；"已批准"≠"已验证"——批准只代表设计决策被接受，不代表机制推断为真。

---

## 6. 协作纪律

1. 决策者纠正 = 语义对齐信号 → 逐条复述确认后再继续；
2. **同类错误重复出现 = 锚定问题** → 强制从物理证据重新推导，禁止从上一版修订文本继续演进（上一版的框架本身就是污染源）；
3. 忘记路径/细节时先查跨项目记忆（Engram）定位真身，不凭对话印象重建；
4. 响应简洁直接，做工程判断；不列选择题推卸判断（不可逆动作除外，须授权）；
5. 修改任何文件前备份；禁止假装成功；未完成事项显式列 Pending——宁可不完成任务，也不伪造闭环。

---

## 7. 与现有体系的关系

| 项 | 说明 |
|---|---|
| Teamwork-Guideline | **暂不修改**（本 spec v0.1 验证前）；其 L0 `.probe/` 层、备份规则（§12）、真理来源声明与本 spec 兼容，probe 产物落 `.probe/` |
| 项目 spec 层级 | 本 spec 为**流程级**文档（how to run projects），项目 spec（如 checkin-spec）为**任务级**文档（what to build）；任务 spec 的流程章节应遵循本流程 spec |
| 并入路径 | v0.1 经 1~2 个项目实证后，修订为 v1.0 并作为候选并入 Teamwork-Guideline（届时由老公决定替换范围） |

## 8. 实证依据（案例索引）

| 教训 | 案例出处 |
|---|---|
| spec 建在残缺探测上 → 实现失败（404） | Trae daily-checkin v0.2（纯静态探测 → 直接 API 复刻失败） |
| 从失败产物框架修补 → 三轮返工 | 同上项目 v0.3/v0.3-r1/v1.0 初稿连续被驳回过程 |
| 凭想象解读先例 → 设计脱节 | v1.0-r1 把"脚本点击"想象为 DOM 点击（真身 = API 复刻，action='clicked'） |
| 先例真身 + 计划文档 = 方法论金矿 | WorkBuddy 项目：`workbuddy_checkin.js`（MD5 锁定 v0）+ `workbuddy-checkin-plan.md` 关键决策"优先 CDP 动态探测而非静态逆向" |
| 动态运行时探测发现静态不可见的机制 | WorkBuddy Phase 1：CDP 注入 fetch/XHR 拦截器 + UI 触发 → 捕获完整 API 规格 |
| 对抗复核发现自我盲区 | Zoo 审计发现 `x-device-id` 替代根因（文档原标记"已排除"依据有缺陷） |

## 9. 修订记录

| 版本 | 日期 | 修订人 | 变更要点 |
|---|---|---|---|
| **v0.1** | 2026-09-14 | 本地 Agent（TRAE IDE） | 初版：在既有"讨论→对齐→spec→执行计划"流程上前置 Probe 阶段（P1 摸底 / P2 全面 / P3 定向穿插）；固化先例盘点第 0 步、证据分级、对抗复核、授权门、回环规则、失败处理原则；显式声明暂不修改 Teamwork-Guideline |